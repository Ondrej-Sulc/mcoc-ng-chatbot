{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --global pnpm && pnpm install && pnpm run build --filter web"
  },
  "deploy": {
    "startCommand": "pnpm run start --filter web"
  }
}
