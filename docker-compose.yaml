services:
  bot:
    build:
      context: .
      target: builder
    volumes:
      - ./src:/usr/src/app/src
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_ENV=development
      - DATABASE_URL=${DATABASE_URL}
    env_file:
      - ./.env
    command: npm run dev
